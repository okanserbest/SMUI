import * as React from 'react';
import { View, Text } from 'react-native';

export interface LikeScreenProps {
}

export function LikeScreen (props: LikeScreenProps) {
    return (
      <View>
         <Text>LikeScreen</Text>
      </View>
    );
}

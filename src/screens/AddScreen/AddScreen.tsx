import * as React from 'react';
import { View, Text } from 'react-native';

export interface AddScreenProps {
}

export function AddScreen (props: AddScreenProps) {
    return (
      <View>
         <Text>AddScreen</Text>
      </View>
    );
}